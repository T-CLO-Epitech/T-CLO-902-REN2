global:
  nodeSelector:
    role: monitoring
  tolerations:
    - key: "role"
      operator: "Equal"
      value: "monitoring"
      effect: "NoSchedule"

server:
  ingress:
    enabled: false
  extraArgs:
    - --insecure