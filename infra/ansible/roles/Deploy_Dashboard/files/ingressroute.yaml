apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: headlamp
  namespace: dashboard
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`headlamp.kubequest.local`)
      kind: Rule
      services:
        - name: headlamp
          port: 80
