apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: grafana
  namespace: monitoring
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`grafana.kubequest.local`)
      kind: Rule
      services:
        - name: kps-grafana
          port: 80
